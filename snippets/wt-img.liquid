{% if img != blank %}<img src="{{ img | image_url: width: 1400 }}" alt="{{ alt }}" class="wt-img wt-img--{{ fit | default: 'cover' }}">{% else %}<div class="wt-img__placeholder">{{ label }}</div>{% endif %}
